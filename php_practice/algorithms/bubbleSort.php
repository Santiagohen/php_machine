<?php


function bubbleSort (array $arr):array{
   $n = count($arr); 
       #  echo "The length of $arr is $n";
   for($i = 0; $i <= $n -1; $i++ ){
 echo "$arr[$i], ";
   }
    return $arr;
}
function optimizeBubbleSort(array $arr):array{
    $n = count($arr)
        for($i )
}


$arr = [1,2,3,4,5];
bubbleSort($arr);
